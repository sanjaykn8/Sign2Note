# Core ML
torch>=2.0.0
torchvision
# Feature extraction
mediapipe>=0.10.0
opencv-python
numpy
# Data
pandas
tqdm
# ONNX export/runtime
onnx
onnxruntime          # CPU runtime
# onnxruntime-gpu    # uncomment if CUDA available
# API service
fastapi
uvicorn[standard]
python-multipart
# LLM notes backend (talks to a locally running `llama-server` via its
# OpenAI-compatible endpoint). Only required for notes_mode="llama_cpp";
# template mode works without it.
openai>=1.0.0
