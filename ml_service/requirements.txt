# Core ML
torch>=2.0.0
torchvision
# Feature extraction
mediapipe>=0.10.0
opencv-python
numpy
# Data
pandas
tqdm
# ONNX export/runtime
onnx
onnxruntime          # CPU runtime
# onnxruntime-gpu    # uncomment if CUDA available
# API service
fastapi
uvicorn[standard]
python-multipart
# LLM notes backend (talks to a locally running Ollama or llama.cpp server
# via its OpenAI-compatible endpoint; see LLM_PROVIDER/LLM_MODEL/LLM_BASE_URL
# in .env.example). Only required for notes_mode="llm"; template mode
# works without it.
openai>=1.0.0
# Optional: loads .env for LLM_PROVIDER/LLM_MODEL/LLM_BASE_URL/
# CONFIDENCE_THRESHOLD without having to export env vars manually.
# Both api.py and infer.py degrade gracefully if this isn't installed.
python-dotenv
